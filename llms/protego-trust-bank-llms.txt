<!-- generated: 2026-08-26 | method: generated | source: API Evangelist enrichment pipeline (public record; provider serves no llms.txt) -->

# Protego Trust Bank

> Protego Trust Bank is a Seattle, Washington digital-asset trust bank built for institutional
> clients — crypto-asset custody, trading, lending/borrowing and issuer services, plus
> custody-related fiduciary services such as staking-as-a-service, protocol governance and
> voting, and fork and airdrop handling.

This file was GENERATED by API Evangelist from the public record on 2026-08-26. It was not
published by Protego Trust Bank. Protego serves no llms.txt of its own — see "Status of the
public surface" below for why no file could be fetched.

## Status of the public surface

- Protego Trust Bank publishes **no public developer portal, API reference, or
  machine-readable contract**. No OpenAPI, Swagger, AsyncAPI, GraphQL SDL, MCP server or
  A2A agent card was found on any host.
- As of 2026-08-26 the company's own web properties are **unreachable over HTTPS**:
  `protegotrust.com`, `www.protegotrust.com` and `protegotrust.co` all terminate the TLS
  handshake with a fatal `internal_error` alert and present no certificate. Plain HTTP
  301-redirects into the same unreachable origin. This reproduces from three independent
  TLS stacks, so it is a server-side condition, not a client or crawler problem.
- `api.protegotrust.com`, `docs.protegotrust.com` and `developer.protegotrust.com` do not
  resolve in DNS.
- The domain itself is actively administered: DNSSEC is signed, SPF and DMARC
  (`p=quarantine`) are published, and MX points at Microsoft 365. The company is
  operating; its web front end is dark.
- Public web archives show the marketing site returning 403/404 continuously since early
  2024; the last successful capture is 2023-12-04.

## Company and regulatory record

- Protego Trust Company received OCC conditional approval in February 2021 to convert into
  a national trust bank. That conditional approval expired in February 2023 after
  pre-conversion requirements (including combined tier 1 capital and liquidity of $54M)
  were not met.
- On 13 February 2026 the OCC granted parent **Protego Holdings Corporation** preliminary
  conditional approval to charter **National Digital Trust Company, Seattle, Washington**
  (Corporate Decision #1366, OCC control numbers 2025-Charter-342009 and
  2025-Waiver-343370, proposed charter no. 25356). The proposed bank's four core business
  lines are a custody platform, a trading platform, a lending/borrowing platform, and an
  issuer services platform. Final approval to commence business has not been granted.

## API surface

- **None published.** Archived Protego marketing material described "API endpoints for
  trading" and multi-source liquidity aggregation for institutional clients, but no
  contract, reference, sandbox, SDK or package was ever published to a public URL or to
  any package registry (npm, PyPI, GitHub all searched — zero first-party results).

## Artifacts in this profile

- `security/protego-trust-bank-domain-security.yml` — probed TLS/DNSSEC/SPF/DMARC/CAA posture
- `well-known/protego-trust-bank-well-known.yml` — full /.well-known/ and contract-discovery probe log
- `packages/protego-trust-bank-packages.yml` — registry search results (zero first-party packages)
- `plans/protego-trust-bank-plans-pricing.yml` — no public plans
- `rate-limits/protego-trust-bank-rate-limits.yml` — no published limits

## Sources

- https://www.occ.gov/topics/charters-and-licensing/interpretations-and-decisions/2026/cd1366.pdf
- https://www.occ.gov/news-issuances/news-releases/2021/nr-occ-2021-19.html
- https://www.linkedin.com/company/protegotrust
- http://web.archive.org/web/20231204010509/https://www.protegotrust.com/
